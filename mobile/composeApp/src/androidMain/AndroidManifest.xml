<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Deliberately offline: no INTERNET permission. The vault never syncs. -->

    <application
        android:allowBackup="false"
        android:fullBackupContent="false"
        android:dataExtractionRules="@null"
        android:label="pass-mgr"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.Material.Light.NoActionBar">
        <!-- Handle config changes in-process instead of recreating the Activity.
             Compose redraws on these without losing state; recreation would
             otherwise discard the in-flight clipboard auto-clear timer (App.kt). -->
        <activity
            android:name="com.passmgr.MainActivity"
            android:exported="true"
            android:label="pass-mgr"
            android:configChanges="orientation|screenSize|smallestScreenSize|screenLayout|keyboardHidden|uiMode|locale|layoutDirection|fontScale|density"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
